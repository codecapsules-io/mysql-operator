{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Overview","text":""},{"location":"#code-capsules-mysql-operator","title":"Code Capsules MySQL Operator","text":"<p>The Code Capsules MySQL Operator is a Kubernetes controller for running highly available MySQL clusters. You can declare clusters, users, and databases as custom resources; the operator handles replication, failover, and day-to-day operations.</p> <p>This project is an independently maintained fork of the open-source bitpoke/mysql-operator. It does not track upstream Bitpoke releases, Helm charts, or documentation.</p>"},{"location":"#what-you-can-do-with-this-operator","title":"What you can do with this operator","text":"<ul> <li>Deploy Percona Server for MySQL clusters (5.7, 8.0, and 8.4 LTS lines).</li> <li>Run multi-replica HA with automatic failover via Orchestrator.</li> <li>Manage users and databases declaratively with <code>MysqlUser</code> and <code>MysqlDatabase</code> resources.</li> <li>Upgrade MySQL versions in a controlled way with operator orchestration (see MySQL versions &amp; upgrades).</li> </ul> <p>Built-in backup and restore features exist in the codebase but are not actively maintained for new deployments. See Legacy backups and the maintenance scope.</p>"},{"location":"#quick-start","title":"Quick start","text":"<p>Install the operator from versioned manifests:</p> <pre><code>export OPERATOR_VERSION=v0.7.0\nkubectl create namespace mysql-operator --dry-run=client -o yaml | kubectl apply -f -\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/crds\"\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/operator\"\nkubectl rollout status statefulset/mysql-operator -n mysql-operator\n</code></pre> <p>Deploy a sample cluster:</p> <pre><code>kubectl apply -f examples/example-cluster-secret.yaml\nkubectl apply -f examples/example-cluster.yaml\n</code></pre> <p>For a full walkthrough, see Getting started.</p>"},{"location":"#documentation-map","title":"Documentation map","text":"Topic Page First-time install and first cluster Getting started Operator install, customize, upgrade Install the operator Move off upstream Helm Migrating from Helm <code>MysqlCluster</code> API reference MysqlCluster PVCs, <code>mysqlConf</code>, tuning Storage &amp; configuration Services and app connection strings Connecting applications MySQL 8.4 and version upgrades MySQL versions &amp; upgrades Prometheus metrics Monitoring"},{"location":"#technology","title":"Technology","text":"<p>Clusters use Percona Server for MySQL for operational features (XtraBackup tooling in sidecars, monitoring integration). Failover topology is managed by Percona Orchestrator, bundled in the operator StatefulSet.</p>"},{"location":"#license","title":"License","text":"<p>Apache License 2.0. See LICENSE and NOTICE in the repository.</p>"},{"location":"connecting-applications/","title":"Connecting applications","text":""},{"location":"connecting-applications/#connecting-applications","title":"Connecting applications","text":"<p>Each <code>MysqlCluster</code> exposes several Kubernetes Services for different connection patterns. You can wire your application Deployments, Jobs, or external tools to these endpoints.</p>"},{"location":"connecting-applications/#service-overview","title":"Service overview","text":"<p>For a cluster named <code>my-cluster</code> in namespace <code>app</code>:</p> Service DNS name Purpose <code>{name}-mysql-master</code> <code>my-cluster-mysql-master.app.svc</code> Writes \u2014 targets the current primary <code>{name}-mysql</code> <code>my-cluster-mysql.app.svc</code> Reads \u2014 all healthy nodes <code>{name}-mysql-replicas</code> <code>my-cluster-mysql-replicas.app.svc</code> Healthy replicas excluding the primary <code>mysql</code> (headless) <code>my-cluster-mysql-0.mysql.app.svc</code> Per-pod DNS for direct pod access <p>All services expose MySQL on port 3306.</p>"},{"location":"connecting-applications/#write-traffic","title":"Write traffic","text":"<p>Point applications that insert, update, or delete data at the master service:</p> <pre><code>my-cluster-mysql-master.app.svc.cluster.local:3306\n</code></pre> <p>The operator updates Service selectors after Orchestrator failover so the master service follows the current primary.</p>"},{"location":"connecting-applications/#read-traffic","title":"Read traffic","text":"<p>For load-balanced reads across healthy replicas:</p> <pre><code>my-cluster-mysql.app.svc.cluster.local:3306\n</code></pre> <p>Replicas with replication lag above <code>spec.maxSlaveLatency</code> are removed from this service.</p>"},{"location":"connecting-applications/#direct-pod-access","title":"Direct pod access","text":"<p>StatefulSet pods are reachable via the headless service <code>mysql</code>:</p> <pre><code>my-cluster-mysql-0.mysql.app.svc.cluster.local\nmy-cluster-mysql-1.mysql.app.svc.cluster.local\n</code></pre> <p>Use this for admin tasks or when you need a specific instance.</p>"},{"location":"connecting-applications/#bootstrap-credentials","title":"Bootstrap credentials","text":"<p>Your application credentials come from the secret referenced by <code>spec.secretName</code> (for example <code>my-secret</code>):</p> Key Description <code>USER</code> Application username (if set at bootstrap) <code>PASSWORD</code> Application password <code>DATABASE</code> Default database name <code>ROOT_PASSWORD</code> Root password (admin use) <p>Construct a connection string from the master service and these values:</p> <pre><code>mysql://USER:PASSWORD@my-cluster-mysql-master.app.svc.cluster.local:3306/DATABASE\n</code></pre> <p>For language-specific drivers, use the hostname, port, user, password, and database from the table above.</p> <p>Note</p> <p><code>USER</code>, <code>PASSWORD</code>, and <code>DATABASE</code> are applied only at cluster bootstrap. For ongoing user management, prefer MysqlUser &amp; MysqlDatabase.</p>"},{"location":"connecting-applications/#operated-secret-internal","title":"Operated secret (internal)","text":"<p>The operator creates <code>{cluster}-mysql-operated</code> with credentials for replication, Orchestrator, metrics, and backups. Do not use these for application traffic unless you understand their scope.</p>"},{"location":"connecting-applications/#example-wiring-a-deployment","title":"Example: wiring a Deployment","text":"<pre><code>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\n  namespace: app\nspec:\n  template:\n    spec:\n      containers:\n        - name: app\n          env:\n            - name: DB_HOST\n              value: my-cluster-mysql-master.app.svc.cluster.local\n            - name: DB_PORT\n              value: \"3306\"\n          envFrom:\n            - secretRef:\n                name: my-secret\n</code></pre> <p>Map <code>USER</code>, <code>PASSWORD</code>, and <code>DATABASE</code> from the secret in your application code or add explicit <code>env</code> entries with <code>secretKeyRef</code>.</p>"},{"location":"connecting-applications/#cross-namespace-access","title":"Cross-namespace access","text":"<p>Services are namespace-scoped. Applications in another namespace should use the fully qualified DNS name:</p> <pre><code>my-cluster-mysql-master.app.svc.cluster.local\n</code></pre> <p>Ensure network policies allow traffic from the client namespace.</p>"},{"location":"connecting-applications/#related-pages","title":"Related pages","text":"<ul> <li>Getting started</li> <li>Orchestrator \u2014 failover behavior affecting the master service</li> <li>Monitoring \u2014 metrics endpoints on cluster pods</li> </ul>"},{"location":"getting-started/","title":"Getting started","text":""},{"location":"getting-started/#getting-started","title":"Getting started","text":"<p>This guide walks you through installing the operator and deploying your first MySQL cluster.</p>"},{"location":"getting-started/#prerequisites","title":"Prerequisites","text":"<p>You will need:</p> <ul> <li>A Kubernetes cluster with <code>kubectl</code> configured</li> <li><code>kubectl</code> 1.14+ (built-in <code>kubectl apply -k</code> uses Kustomize)</li> <li>Cluster-admin permissions to install CRDs and cluster-scoped RBAC</li> </ul> <p>Helm is not required. This fork installs the operator with <code>kubectl apply -k</code> against manifests in <code>deploy/manifests/</code>.</p>"},{"location":"getting-started/#install-the-operator","title":"Install the operator","text":"<p>Pick the operator release that matches the git tag you are deploying (directory names include the <code>v</code> prefix, for example <code>v0.7.0</code>).</p> <pre><code>export OPERATOR_VERSION=v0.7.0\nexport OPERATOR_NAMESPACE=mysql-operator\n\nkubectl create namespace \"${OPERATOR_NAMESPACE}\" --dry-run=client -o yaml | kubectl apply -f -\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/crds\"\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/operator\"\n</code></pre> <p>Apply order matters: CRDs first, then operator resources.</p>"},{"location":"getting-started/#verify-the-install","title":"Verify the install","text":"<pre><code>kubectl get crd | grep mysql.presslabs.org\nkubectl get pods,svc -n mysql-operator\nkubectl rollout status statefulset/mysql-operator -n mysql-operator\n</code></pre> <p>You should see four CRDs (<code>mysqlclusters</code>, <code>mysqlusers</code>, <code>mysqldatabases</code>, <code>mysqlbackups</code>) and a running <code>mysql-operator</code> StatefulSet in the <code>mysql-operator</code> namespace.</p> <p>For customization options (images, operator flags, orchestrator password), see Install the operator.</p>"},{"location":"getting-started/#deploy-your-first-cluster","title":"Deploy your first cluster","text":"<p>Cluster resources live in your application namespace, not in <code>mysql-operator</code>.</p>"},{"location":"getting-started/#1-create-a-credentials-secret","title":"1. Create a credentials secret","text":"<p>The cluster needs a secret with at least <code>ROOT_PASSWORD</code> (base64-encoded). Optional bootstrap fields <code>USER</code>, <code>PASSWORD</code>, and <code>DATABASE</code> create an application user and database on first start.</p> <pre><code>apiVersion: v1\nkind: Secret\nmetadata:\n  name: my-secret\ntype: Opaque\ndata:\n  ROOT_PASSWORD: bm90LXNvLXNlY3VyZQ==   # base64-encoded password\n</code></pre> <p>See <code>examples/example-cluster-secret.yaml</code> for a full example.</p> <p>Bootstrap-only fields</p> <p><code>USER</code>, <code>PASSWORD</code>, and <code>DATABASE</code> are used only when the cluster is first initialized. Changing them later does not update the MySQL server.</p>"},{"location":"getting-started/#2-create-a-mysqlcluster","title":"2. Create a MysqlCluster","text":"<pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlCluster\nmetadata:\n  name: my-cluster\nspec:\n  replicas: 2\n  secretName: my-secret\n  mysqlVersion: \"8.0\"\n</code></pre> <p>Apply both files:</p> <pre><code>kubectl apply -f examples/example-cluster-secret.yaml\nkubectl apply -f examples/example-cluster.yaml\n</code></pre> <p>Adapt names, namespace, storage class, and <code>mysqlVersion</code> before production use. For Percona 8.4, set <code>mysqlVersion: \"8.4\"</code> \u2014 see MySQL versions &amp; upgrades.</p>"},{"location":"getting-started/#3-check-cluster-status","title":"3. Check cluster status","text":"<pre><code>kubectl get mysql\nkubectl describe mysql my-cluster\n</code></pre> <p>When the cluster is ready, <code>status.conditions</code> includes <code>Ready=True</code> and <code>status.readyNodes</code> matches your replica count.</p>"},{"location":"getting-started/#uninstall-the-operator","title":"Uninstall the operator","text":"<p>Removing the operator does not delete <code>MysqlCluster</code> resources in other namespaces.</p> <pre><code>export OPERATOR_VERSION=v0.7.0\n\nkubectl delete -k \"deploy/manifests/${OPERATOR_VERSION}/operator\"\n</code></pre> <p>Delete CRDs only when no custom resources remain:</p> <pre><code>kubectl delete -k \"deploy/manifests/${OPERATOR_VERSION}/crds\"\n</code></pre> <p>Warning</p> <p>Deleting CRDs removes all <code>MysqlCluster</code>, <code>MysqlUser</code>, <code>MysqlDatabase</code>, and <code>MysqlBackup</code> objects cluster-wide.</p>"},{"location":"getting-started/#next-steps","title":"Next steps","text":"<ul> <li>MysqlCluster \u2014 full configuration reference</li> <li>Connecting applications \u2014 service endpoints and connection strings</li> <li>Orchestrator \u2014 failover UI and topology</li> <li>Migrating from Helm \u2014 if you run the legacy Bitpoke Helm chart</li> </ul>"},{"location":"install-operator/","title":"Install the operator","text":""},{"location":"install-operator/#install-the-operator","title":"Install the operator","text":"<p>This page describes how to install, verify, customize, and upgrade the operator using versioned Kubernetes manifests under <code>deploy/manifests/</code>.</p>"},{"location":"install-operator/#layout","title":"Layout","text":"<p>Each release is stored under a semver directory named after the git tag:</p> <pre><code>deploy/manifests/\n  v0.6.3/          # Frozen legacy baseline (former Helm chart defaults)\n  v0.7.0/          # Current release\n    crds/          # CustomResourceDefinitions \u2014 apply first\n    operator/      # RBAC, StatefulSet, Services, ConfigMaps \u2014 apply second\n    kustomization.yaml\n</code></pre> <p>The <code>operator/</code> kustomization sets the namespace to <code>mysql-operator</code>.</p>"},{"location":"install-operator/#fresh-install","title":"Fresh install","text":"<p>From a checkout of this repository (or a release archive that includes <code>deploy/manifests/</code>):</p> <pre><code>export OPERATOR_VERSION=v0.7.0\nexport OPERATOR_NAMESPACE=mysql-operator\n\nkubectl create namespace \"${OPERATOR_NAMESPACE}\" --dry-run=client -o yaml | kubectl apply -f -\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/crds\"\nkubectl apply -k \"deploy/manifests/${OPERATOR_VERSION}/operator\"\n</code></pre> <p>One-step alternative (same result):</p> <pre><code>kubectl apply -k deploy/manifests/v0.7.0\n</code></pre>"},{"location":"install-operator/#verify","title":"Verify","text":"<pre><code>kubectl get crd | grep mysql.presslabs.org\nkubectl get pods,svc -n mysql-operator\nkubectl rollout status statefulset/mysql-operator -n mysql-operator\n</code></pre> <p>The operator runs as a StatefulSet named <code>mysql-operator</code> with two containers: the operator controller and orchestrator.</p>"},{"location":"install-operator/#container-images","title":"Container images","text":"<p>Default manifests pin images to <code>ghcr.io/codecapsules-io/*</code> at the release tag, for example:</p> Component Example image Operator <code>ghcr.io/codecapsules-io/mysql-operator:v0.7.0</code> Orchestrator <code>ghcr.io/codecapsules-io/mysql-operator-orchestrator:v0.7.0</code> Sidecar 5.7 <code>ghcr.io/codecapsules-io/mysql-operator-sidecar-5.7:v0.7.0</code> Sidecar 8.0 <code>ghcr.io/codecapsules-io/mysql-operator-sidecar-8.0:v0.7.0</code> Sidecar 8.4 <code>ghcr.io/codecapsules-io/mysql-operator-sidecar-8.4:v0.7.0</code> mysqld_exporter <code>docker.io/prom/mysqld-exporter:v0.16.0</code> <p>CI publishes operator images to GitHub Container Registry (<code>ghcr.io/codecapsules-io/...</code>). To use a different registry, edit image references in <code>deploy/manifests/&lt;version&gt;/operator/statefulset.yaml</code> before applying.</p>"},{"location":"install-operator/#customizing-your-install","title":"Customizing your install","text":"<p>Edit files under <code>deploy/manifests/&lt;version&gt;/operator/</code> before applying. Common changes:</p> File What to change <code>statefulset.yaml</code> Image repositories/tags, operator <code>args</code>, replica count <code>orchestrator-secret.yaml</code> Orchestrator topology credentials (<code>TOPOLOGY_USER</code>, <code>TOPOLOGY_PASSWORD</code>) <code>orchestrator-config.yaml</code> Orchestrator JSON settings <code>clusterrole.yaml</code> RBAC if you fork with new API resources"},{"location":"install-operator/#orchestrator-topology-password","title":"Orchestrator topology password","text":"<p>The operator and Orchestrator use a shared topology user to connect to MySQL pods. Credentials live in the <code>mysql-operator-orc</code> secret (<code>orchestrator-secret.yaml</code>).</p> <p>Keep topology credentials stable on upgrade</p> <p>If you change the topology password in manifests without updating credentials already stored in cluster secrets, Orchestrator can lose communication with existing clusters. Set explicit credentials in <code>orchestrator-secret.yaml</code> and keep them across upgrades.</p>"},{"location":"install-operator/#operator-flags","title":"Operator flags","text":"<p>Important defaults in <code>v0.7.0</code> <code>statefulset.yaml</code>:</p> <ul> <li><code>--sidecar-mysql84-image</code> \u2014 required for Percona 8.4 clusters</li> <li><code>--failover-before-shutdown=true</code> \u2014 triggers failover in the MySQL preStop hook</li> <li><code>--orchestrator-uri=http://mysql-operator.mysql-operator/api</code></li> </ul> <p>For the full flag list, see Operator configuration.</p>"},{"location":"install-operator/#upgrade-an-existing-install","title":"Upgrade an existing install","text":"<ol> <li>Apply the new version's CRDs: <code>kubectl apply -k deploy/manifests/&lt;new-version&gt;/crds</code></li> <li>Apply the new operator manifests: <code>kubectl apply -k deploy/manifests/&lt;new-version&gt;/operator</code></li> <li>Wait for rollout: <code>kubectl rollout status statefulset/mysql-operator -n mysql-operator</code></li> </ol> <p>Review Operator upgrades and MySQL versions &amp; upgrades before crossing minor versions.</p>"},{"location":"install-operator/#uninstall","title":"Uninstall","text":"<pre><code>export OPERATOR_VERSION=v0.7.0\n\nkubectl delete -k \"deploy/manifests/${OPERATOR_VERSION}/operator\"\nkubectl delete -k \"deploy/manifests/${OPERATOR_VERSION}/crds\"   # only when CRs are gone\n</code></pre>"},{"location":"install-operator/#maintainer-prepare-manifests-for-a-new-release","title":"Maintainer: prepare manifests for a new release","text":"<p>See the maintainer section in <code>deploy/manifests/README.md</code> for <code>make deploy.crds</code>, copying the <code>operator/</code> tree, and validation steps.</p>"},{"location":"legacy-backups/","title":"Legacy backups","text":""},{"location":"legacy-backups/#legacy-backups","title":"Legacy backups","text":"<p>The operator includes a built-in backup subsystem: on-demand <code>MysqlBackup</code> resources, scheduled backups via <code>spec.backupSchedule</code>, and cluster initialization from <code>spec.initBucketURL</code>. This functionality is not actively maintained in the Code Capsules fork.</p> <p>Maintenance scope</p> <p>For new deployments, use external backup solutions or platform-level backup. See the maintenance scope.</p> <p>This page exists so operators running legacy configurations can understand what the CRDs do and where to find examples.</p>"},{"location":"legacy-backups/#what-still-exists-in-the-codebase","title":"What still exists in the codebase","text":"Resource / field Purpose <code>MysqlBackup</code> CR Request a one-off backup job <code>spec.backupSchedule</code> Cron-based scheduled backups (6-field cron with seconds) <code>spec.backupURL</code> Remote storage destination (for example <code>s3://bucket/path/</code>) <code>spec.backupSecretName</code> Credentials for object storage (Rclone backends) <code>spec.initBucketURL</code> Restore / clone from an existing backup at cluster creation <p>Controllers and sidecar tooling for XtraBackup and Rclone remain in the repository but do not receive active feature work.</p>"},{"location":"legacy-backups/#examples","title":"Examples","text":"<p>Reference manifests (use at your own risk):</p> <ul> <li><code>examples/example-backup.yaml</code></li> <li><code>examples/example-backup-secret.yaml</code></li> <li><code>examples/example-cluster-init.yaml</code></li> </ul>"},{"location":"legacy-backups/#on-demand-backup-legacy","title":"On-demand backup (legacy)","text":"<pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlBackup\nmetadata:\n  name: my-cluster-backup\nspec:\n  clusterName: my-cluster\n</code></pre> <p>The cluster must have <code>backupURL</code> and <code>backupSecretName</code> configured.</p> <pre><code>kubectl get mysqlbackup\nkubectl describe mysqlbackup my-cluster-backup\n</code></pre>"},{"location":"legacy-backups/#scheduled-backups-legacy","title":"Scheduled backups (legacy)","text":"<pre><code>spec:\n  backupSchedule: \"0 0 2 * * *\"   # daily at 02:00:00\n  backupURL: s3://my-bucket/my-cluster/\n  backupSecretName: my-cluster-backup-secret\n  backupRemoteDeletePolicy: retain\n</code></pre> <p>Disable scheduled backups by setting <code>backupSchedule: \"\"</code>.</p>"},{"location":"legacy-backups/#restore-via-new-cluster-legacy","title":"Restore via new cluster (legacy)","text":"<p>Create a new <code>MysqlCluster</code> with:</p> <pre><code>spec:\n  initBucketURL: s3://my-bucket/my-cluster/backup.xtrabackup.gz\n  initBucketSecretName: my-cluster-backup-secret\n</code></pre>"},{"location":"legacy-backups/#recommendations","title":"Recommendations","text":"<ul> <li>New clusters: plan backups outside this operator (cloud provider snapshots, Percona XtraBackup in CronJobs, logical dumps, and similar).</li> <li>Existing clusters: test restore procedures regularly; do not assume backward compatibility across operator upgrades.</li> <li>MySQL upgrades: always take a backup before changing <code>spec.mysqlVersion</code> \u2014 see MySQL versions &amp; upgrades.</li> </ul>"},{"location":"legacy-backups/#related-pages","title":"Related pages","text":"<ul> <li>MysqlCluster \u2014 backup-related spec fields</li> <li>Maintenance scope</li> </ul>"},{"location":"migrate-from-helm/","title":"Migrating from Helm","text":""},{"location":"migrate-from-helm/#migrating-from-helm","title":"Migrating from Helm","text":"<p>Helm charts were removed in v0.7.0. If you currently run the operator via the upstream Bitpoke Helm chart (or an older fork release at v0.6.3 or earlier), you can migrate to manifest-based installs.</p>"},{"location":"migrate-from-helm/#what-changed","title":"What changed","text":"Before (Helm) After (this fork) <code>helm install</code> from <code>helm-charts.bitpoke.io</code> <code>kubectl apply -k deploy/manifests/v0.7.0</code> Chart <code>values.yaml</code> for images and flags Edit YAML under <code>deploy/manifests/&lt;version&gt;/operator/</code> <code>bitpoke/*</code> images <code>codecapsules-io/*</code> images (by default) No Percona 8.4 sidecar flag <code>--sidecar-mysql84-image</code> on operator <p>Frozen v0.6.3 manifests under <code>deploy/manifests/v0.6.3/</code> mirror what the last Helm chart deployed. Use them as a reference when comparing your Helm values to the new layout.</p>"},{"location":"migrate-from-helm/#typical-migration-path","title":"Typical migration path","text":""},{"location":"migrate-from-helm/#1-record-your-helm-values","title":"1. Record your Helm values","text":"<p>Note image repositories/tags, <code>extraArgs</code>, orchestrator topology password, <code>watchNamespace</code>, and any custom sidecar or exporter images from your Helm release.</p>"},{"location":"migrate-from-helm/#2-compare-with-v063-manifests","title":"2. Compare with v0.6.3 manifests","text":"<p>Open <code>deploy/manifests/v0.6.3/operator/statefulset.yaml</code>. Defaults use <code>docker.io/bitpoke/*:v0.6.3</code> and <code>prom/mysqld-exporter:v0.13.0</code>.</p>"},{"location":"migrate-from-helm/#3-prepare-v070-manifests","title":"3. Prepare v0.7.0 manifests","text":"<p>Before applying, edit <code>deploy/manifests/v0.7.0/operator/</code> if you need non-default:</p> <ul> <li>Image registries or tags</li> <li>Orchestrator topology credentials (set explicitly in <code>orchestrator-secret.yaml</code>)</li> <li>Operator CLI flags (for example <code>--namespace</code> to limit watch scope)</li> <li><code>--sidecar-mysql84-image</code> if you run 8.4 clusters</li> </ul>"},{"location":"migrate-from-helm/#4-uninstall-the-helm-release","title":"4. Uninstall the Helm release","text":"<pre><code>helm uninstall &lt;release-name&gt; -n mysql-operator\n</code></pre> <p>This removes the operator workload and RBAC created by Helm. It does not remove CRDs or <code>MysqlCluster</code> resources in application namespaces by default.</p>"},{"location":"migrate-from-helm/#5-apply-v070-manifests","title":"5. Apply v0.7.0 manifests","text":"<pre><code>kubectl apply -k deploy/manifests/v0.7.0/crds\nkubectl apply -k deploy/manifests/v0.7.0/operator\nkubectl rollout status statefulset/mysql-operator -n mysql-operator\n</code></pre>"},{"location":"migrate-from-helm/#6-verify","title":"6. Verify","text":"<pre><code>kubectl get crd | grep mysql.presslabs.org\nkubectl get pods -n mysql-operator\nkubectl get mysql -A\n</code></pre> <p>Existing <code>MysqlCluster</code> resources should reconcile under the new operator. Check <code>kubectl describe mysql &lt;name&gt;</code> for <code>Ready=True</code>.</p>"},{"location":"migrate-from-helm/#crd-changes","title":"CRD changes","text":"<p>Applying <code>v0.7.0</code> CRDs updates API schemas in place. Review CHANGELOG for new fields (<code>sidecarImage</code>, <code>volumeSpec.keepAfterDelete</code>, <code>status.appliedMysqlVersion</code>).</p>"},{"location":"migrate-from-helm/#mysql-version-upgrades-after-migration","title":"MySQL version upgrades after migration","text":"<p>Migration to v0.7.0 is an operator upgrade. MySQL server upgrades (for example 8.0 \u2192 8.4) are separate \u2014 see MySQL versions &amp; upgrades.</p>"},{"location":"migrate-from-helm/#further-reading","title":"Further reading","text":"<ul> <li>Install the operator</li> <li>Operator upgrades</li> </ul>"},{"location":"monitoring/","title":"Monitoring","text":""},{"location":"monitoring/#monitoring","title":"Monitoring","text":"<p>The operator and each MySQL cluster expose Prometheus-compatible metrics.</p>"},{"location":"monitoring/#cluster-pod-metrics-mysqld_exporter","title":"Cluster pod metrics (mysqld_exporter)","text":"<p>Every MySQL pod runs a mysqld_exporter sidecar that exposes metrics on port 9125 at path <code>/metrics</code>.</p> <p>Pod template annotations (set by the operator):</p> <pre><code>prometheus.io/scrape: \"true\"\nprometheus.io/port: \"9125\"\n</code></pre>"},{"location":"monitoring/#scrape-with-prometheus-operator","title":"Scrape with Prometheus Operator","text":"<p>Example <code>PodMonitor</code> for clusters in namespace <code>app</code>:</p> <pre><code>apiVersion: monitoring.coreos.com/v1\nkind: PodMonitor\nmetadata:\n  name: mysql-clusters\n  namespace: monitoring\nspec:\n  namespaceSelector:\n    matchNames:\n      - app\n  selector:\n    matchLabels:\n      app.kubernetes.io/managed-by: mysql.presslabs.org\n  podMetricsEndpoints:\n    - port: prometheus\n      path: /metrics\n      interval: 30s\n</code></pre> <p>Adjust the namespace selector and labels to match your deployment. MySQL pods label the metrics port as <code>prometheus</code>.</p>"},{"location":"monitoring/#extra-exporter-flags","title":"Extra exporter flags","text":"<p>Pass additional mysqld_exporter arguments per cluster:</p> <pre><code>spec:\n  metricsExporterExtraArgs:\n    - --collect.info_schema.processlist\n</code></pre> <p>See the mysqld_exporter documentation for available collectors.</p>"},{"location":"monitoring/#operator-metrics","title":"Operator metrics","text":"<p>The operator controller serves metrics on port 8080 (flag <code>--metrics-addr</code>, default <code>:8080</code>). The <code>mysql-operator</code> Service maps external port 9125 to this container port for scraping convenience.</p> <p>Example <code>ServiceMonitor</code> for the operator:</p> <pre><code>apiVersion: monitoring.coreos.com/v1\nkind: ServiceMonitor\nmetadata:\n  name: mysql-operator\n  namespace: monitoring\nspec:\n  namespaceSelector:\n    matchNames:\n      - mysql-operator\n  selector:\n    matchLabels:\n      app.kubernetes.io/name: mysql-operator\n  endpoints:\n    - port: prometheus\n      path: /metrics\n      interval: 30s\n</code></pre> <p>Disable operator metrics with <code>--metrics-addr=0</code> in the StatefulSet args.</p>"},{"location":"monitoring/#health-probes","title":"Health probes","text":"<p>The operator container exposes <code>/healthz</code> and <code>/readyz</code> on port 8081 (<code>--healthz-addr</code>).</p>"},{"location":"monitoring/#related-pages","title":"Related pages","text":"<ul> <li>Operator configuration \u2014 <code>--metrics-exporter-image</code>, <code>--metrics-addr</code></li> <li>MysqlCluster \u2014 <code>metricsExporterExtraArgs</code>, <code>podSpec.metricsExporterResources</code></li> </ul>"},{"location":"mysql-cluster/","title":"MysqlCluster","text":""},{"location":"mysql-cluster/#mysqlcluster","title":"MysqlCluster","text":"<p>The <code>MysqlCluster</code> custom resource (<code>mysql.presslabs.org/v1alpha1</code>) describes a highly available MySQL cluster. The operator creates a StatefulSet, Services, secrets, config maps, and related objects from your spec.</p>"},{"location":"mysql-cluster/#minimal-example","title":"Minimal example","text":"<pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlCluster\nmetadata:\n  name: my-cluster\nspec:\n  replicas: 2\n  secretName: my-secret\n  mysqlVersion: \"8.0\"\n</code></pre> <p>Apply a bootstrap secret first \u2014 see Getting started.</p> <p>Cluster name length</p> <p>Keep the cluster name reasonably short. Very long names can fail to register with Orchestrator.</p>"},{"location":"mysql-cluster/#credentials-secret-specsecretname","title":"Credentials secret (<code>spec.secretName</code>)","text":"<p>The secret must exist in the same namespace as the cluster and include at least:</p> Key Required Purpose <code>ROOT_PASSWORD</code> Yes MySQL root password (base64-encoded) <code>USER</code> No Application user created at bootstrap <code>PASSWORD</code> No Password for <code>USER</code> <code>DATABASE</code> No Database created at bootstrap <p>All values must be base64-encoded in the Secret <code>data</code> field.</p> <p>The operator also creates a separate operated secret (<code>{cluster}-mysql-operated</code>) with internal credentials for replication, metrics, Orchestrator, and backups. See Connecting applications.</p>"},{"location":"mysql-cluster/#spec-reference","title":"Spec reference","text":""},{"location":"mysql-cluster/#core-fields","title":"Core fields","text":"Field Description Default <code>replicas</code> Number of MySQL pods <code>0</code> if unset (set explicitly in production) <code>secretName</code> Bootstrap credentials secret Required <code>mysqlVersion</code> Server version line (<code>\"5.7\"</code>, <code>\"8.0\"</code>, <code>\"8.4\"</code>, or exact semver) <code>5.7</code> <code>image</code> Custom server image (overrides catalog; <code>mysqlVersion</code> still required) From catalog <code>sidecarImage</code> Per-cluster sidecar override Operator flags <code>minAvailable</code> PodDisruptionBudget minimum available <code>50%</code> <code>readOnly</code> Put cluster in read-only mode (best-effort) <code>false</code> <code>serverIDOffset</code> Offset added to StatefulSet ordinal for <code>server_id</code> <code>0</code>"},{"location":"mysql-cluster/#initialization-and-cloning","title":"Initialization and cloning","text":"Field Description <code>initBucketURL</code> Object storage URL with an XtraBackup to seed data (for example <code>s3://bucket/backup.xtrabackup.gz</code>) <code>initBucketSecretName</code> Secret with storage credentials for <code>initBucketURL</code> <code>initBucketURI</code> Deprecated \u2014 use <code>initBucketURL</code>"},{"location":"mysql-cluster/#storage","title":"Storage","text":"Field Description <code>volumeSpec</code> PVC, HostPath, or EmptyDir for data; see Storage &amp; configuration <code>volumeSpec.keepAfterDelete</code> When <code>true</code>, retain PVCs after the <code>MysqlCluster</code> is deleted"},{"location":"mysql-cluster/#mysql-tuning","title":"MySQL tuning","text":"Field Description <code>mysqlConf</code> Key-value map merged into <code>my.cnf</code> under <code>[mysqld]</code> <code>podSpec</code> Pod template overrides (resources, affinity, extra containers, and more) <code>maxSlaveLatency</code> Remove replicas from read service when lag exceeds this many seconds <code>queryLimits</code> pt-kill parameters for long-running or idle queries"},{"location":"mysql-cluster/#legacy-backup-fields","title":"Legacy backup fields","text":"<p>These fields drive the built-in backup subsystem, which is not actively maintained. See Legacy backups.</p> Field Description <code>backupSchedule</code> Cron expression with seconds for scheduled backups <code>backupURL</code> Remote storage URL for backups <code>backupSecretName</code> Credentials for backup storage <code>backupRemoteDeletePolicy</code> <code>retain</code> or <code>delete</code> remote backup data <code>backupScheduleJobsHistoryLimit</code> Number of scheduled backup jobs to keep <code>backupCompressCommand</code> / <code>backupDecompressCommand</code> Custom compress/decompress commands <code>rcloneExtraArgs</code>, <code>xbstreamExtraArgs</code>, <code>xtrabackupExtraArgs</code>, etc. Extra arguments for backup tooling"},{"location":"mysql-cluster/#status-reference","title":"Status reference","text":"Field Description <code>readyNodes</code> Count of ready replicas <code>appliedMysqlVersion</code> MySQL version confirmed on all pods via <code>SELECT VERSION()</code> <code>conditions</code> Cluster health and operational state <code>nodes</code> Per-node status from Orchestrator"},{"location":"mysql-cluster/#conditions","title":"Conditions","text":"Type Meaning <code>Ready</code> StatefulSet is ready and cluster is operational <code>PendingFailoverAck</code> Orchestrator is waiting for failover acknowledgment <code>ReadOnly</code> Cluster is in read-only mode <code>FailoverInProgress</code> Orchestrator is performing a failover <code>UpgradeBlocked</code> Requested <code>mysqlVersion</code> change is invalid (downgrade or skipped LTS line) <p>When <code>UpgradeBlocked</code> is <code>True</code>, correct <code>spec.mysqlVersion</code> to a valid path (for example 8.0 before 8.4). See MySQL versions &amp; upgrades.</p>"},{"location":"mysql-cluster/#short-names-and-kubectl","title":"Short names and kubectl","text":"<pre><code>kubectl get mysql\nkubectl describe mysql my-cluster\nkubectl get mysqlcluster my-cluster -o yaml\n</code></pre>"},{"location":"mysql-cluster/#examples","title":"Examples","text":"<p>Commented samples live in the repository:</p> <ul> <li><code>examples/example-cluster.yaml</code></li> <li><code>examples/example-cluster-84.yaml</code> \u2014 Percona 8.4</li> </ul>"},{"location":"mysql-cluster/#related-pages","title":"Related pages","text":"<ul> <li>Storage &amp; configuration</li> <li>Connecting applications</li> <li>MySQL versions &amp; upgrades</li> </ul>"},{"location":"mysql-user-and-database/","title":"MysqlUser & MysqlDatabase","text":""},{"location":"mysql-user-and-database/#mysqluser-and-mysqldatabase","title":"MysqlUser and MysqlDatabase","text":"<p>Beyond bootstrap credentials in the cluster secret, you can manage MySQL users and databases declaratively with <code>MysqlUser</code> and <code>MysqlDatabase</code> resources.</p> <p>Both resources reference a <code>MysqlCluster</code> via <code>spec.clusterRef</code> (name and namespace). By default, the cluster must be in the same namespace unless the operator runs with <code>--allow-cross-namespace-user</code> or <code>--allow-cross-namespace-database</code>.</p>"},{"location":"mysql-user-and-database/#mysqluser","title":"MysqlUser","text":"<p>Creates a MySQL user with grants on specified schemas and tables.</p>"},{"location":"mysql-user-and-database/#example","title":"Example","text":"<p>Password secret:</p> <pre><code>apiVersion: v1\nkind: Secret\nmetadata:\n  name: my-user-password\ndata:\n  PASSWORD: bXlzcWwtcGFzc3dvcmQtZm9yLXVzZXI=\n</code></pre> <p>User resource:</p> <pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlUser\nmetadata:\n  name: my-user\nspec:\n  user: user-name-in-mysql\n  clusterRef:\n    name: my-cluster\n    namespace: default\n  password:\n    name: my-user-password\n    key: PASSWORD\n  allowedHosts:\n    - localhost\n    - \"%\"\n  permissions:\n    - schema: db-name-in-mysql\n      tables: [\"*\"]\n      permissions:\n        - SELECT\n        - INSERT\n        - UPDATE\n        - DELETE\n</code></pre> <p>Apply:</p> <pre><code>kubectl apply -f examples/example-user.yaml\n</code></pre>"},{"location":"mysql-user-and-database/#deletion-policy","title":"Deletion policy","text":"<p>By default, deleting the <code>MysqlUser</code> CR removes the MySQL user. To retain the user in MySQL:</p> <pre><code>metadata:\n  annotations:\n    mysql-operator.presslabs.org/resourceDeletionPolicy: retain\n</code></pre>"},{"location":"mysql-user-and-database/#status","title":"Status","text":"<p>When reconciled, the resource reports a <code>Ready</code> condition:</p> <pre><code>kubectl get mysqluser\nkubectl describe mysqluser my-user\n</code></pre>"},{"location":"mysql-user-and-database/#mysqldatabase","title":"MysqlDatabase","text":"<p>Creates a database on the referenced cluster.</p> <pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlDatabase\nmetadata:\n  name: my-database\nspec:\n  database: db-name-in-mysql\n  clusterRef:\n    name: my-cluster\n    namespace: default\n</code></pre> <p>Optional fields <code>characterSet</code> and <code>collation</code> can be set in the full API schema.</p>"},{"location":"mysql-user-and-database/#deletion-policy_1","title":"Deletion policy","text":"<p>Same annotation as <code>MysqlUser</code>:</p> <pre><code>metadata:\n  annotations:\n    mysql-operator.presslabs.org/resourceDeletionPolicy: retain\n</code></pre>"},{"location":"mysql-user-and-database/#when-to-use-which","title":"When to use which","text":"Approach Best for Bootstrap secret (<code>USER</code> / <code>PASSWORD</code> / <code>DATABASE</code>) Initial app user at cluster creation <code>MysqlUser</code> / <code>MysqlDatabase</code> Ongoing declarative management, multiple apps, GitOps workflows"},{"location":"mysql-user-and-database/#auth-plugin-note-84","title":"Auth plugin note (8.4)","text":"<p>Users created on MySQL 8.0 with <code>mysql_native_password</code> must be migrated before upgrading to 8.4. See MySQL versions &amp; upgrades.</p>"},{"location":"mysql-user-and-database/#related-pages","title":"Related pages","text":"<ul> <li>MysqlCluster</li> <li>Operator configuration \u2014 cross-namespace flags</li> <li><code>examples/example-user.yaml</code></li> <li><code>examples/example-database.yaml</code></li> </ul>"},{"location":"mysql-version-profiles/","title":"Version profiles","text":""},{"location":"mysql-version-profiles/#version-profiles","title":"Version profiles","text":"<p>Server images are resolved by the image resolver (see MySQL versions &amp; upgrades): <code>spec.image</code>, CLI overrides, catalog file, then built-in constants.</p> <p>Profiles describe version-line behavior: replication SQL dialect, grant metadata, operator <code>my.cnf</code> fragments, sidecar logical key, and validation. Built-in Percona profiles are:</p> Profile key MySQL line <code>percona-5.7</code> 5.7.x <code>percona-8.0</code> 8.0.x through 8.3.x <code>percona-8.4</code> 8.4 LTS and newer <p>Unrecognized semvers fall back to <code>fallback-unknown</code> with limited guarantees.</p>"},{"location":"mysql-version-profiles/#sidecar-mapping","title":"Sidecar mapping","text":"<p>Each profile maps to a dedicated sidecar image configured on the operator:</p> Profile Operator flag <code>percona-5.7</code> <code>--sidecar-image</code> <code>percona-8.0</code> <code>--sidecar-mysql8-image</code> <code>percona-8.4</code> <code>--sidecar-mysql84-image</code> <p>There is no cross-profile fallback. If you run 8.4 clusters, you must set <code>--sidecar-mysql84-image</code> on the operator (included in <code>deploy/manifests/v0.7.0</code> defaults).</p> <p>Override per cluster with <code>spec.sidecarImage</code> when needed.</p>"},{"location":"mysql-version-profiles/#orchestrator-and-mysqld_exporter","title":"Orchestrator and mysqld_exporter","text":"<p>Discovery and failover depend on the Orchestrator build in this repository's Docker image (<code>images/mysql-operator-orchestrator/Dockerfile</code>), built from percona/orchestrator at a pinned commit so topology discovery uses MySQL 8.4+ replication SQL (<code>SHOW REPLICA STATUS</code>, etc.). Older openark/orchestrator 3.2.x binaries hit parse errors on 8.4.</p> <p>Override the orchestrator container image in the operator deployment if you use a different build \u2014 see Install the operator.</p> <p>The mysqld_exporter image is set with <code>--metrics-exporter-image</code> (default <code>prom/mysqld-exporter:v0.16.0</code> in versioned manifests). See Monitoring.</p>"},{"location":"mysql-version-profiles/#related-pages","title":"Related pages","text":"<ul> <li>MySQL versions &amp; upgrades</li> <li>Operator configuration</li> </ul>"},{"location":"mysql-versions-and-upgrades/","title":"MySQL versions & upgrades","text":""},{"location":"mysql-versions-and-upgrades/#mysql-versions-and-upgrades","title":"MySQL versions and upgrades","text":"<p>This operator resolves the Percona (or other) server image for a <code>MysqlCluster</code> in this order:</p> <ol> <li><code>spec.image</code> on the cluster (highest priority).</li> <li><code>--mysql-versions-to-image</code> CLI entries for the exact semver string (for example <code>8.4.0=...</code>).</li> <li>Lines from <code>--mysql-version-catalog-file</code> (for example a ConfigMap mounted as a file), same <code>semver=image</code> format.</li> <li>Built-in defaults in the operator source (<code>pkg/util/constants/constants.go</code>).</li> </ol> <p>Sidecar images are chosen by resolving the server semver to a profile (<code>percona-5.7</code>, <code>percona-8.0</code>, <code>percona-8.4</code>) and mapping that to operator flags (<code>--sidecar-image</code>, <code>--sidecar-mysql8-image</code>, <code>--sidecar-mysql84-image</code>). Each profile uses only its configured image \u2014 there is no fallback to another profile's sidecar (for example 8.4 does not use the 8.0 sidecar when <code>--sidecar-mysql84-image</code> is empty). You can override with <code>spec.sidecarImage</code> on a cluster. Cluster validation rejects a missing sidecar image for the requested version.</p> <p>Version-specific SQL and <code>my.cnf</code> behavior is defined in built-in profiles; see Version profiles.</p>"},{"location":"mysql-versions-and-upgrades/#built-in-version-catalog","title":"Built-in version catalog","text":"<p>Short tags in <code>spec.mysqlVersion</code> map to pinned patch releases:</p> Short tag Resolves to <code>\"5.7\"</code> <code>5.7.35</code> <code>\"8.0\"</code> <code>8.0.20</code> <code>\"8.4\"</code> <code>8.4.8</code> <p>You can also set an exact semver (for example <code>8.0.34</code>) when using a custom <code>spec.image</code>.</p>"},{"location":"mysql-versions-and-upgrades/#short-tags-vs-exact-versions","title":"Short tags vs exact versions","text":"<p>Built-in catalog images accept short tags such as <code>\"8.0\"</code> or <code>\"8.4\"</code>. For custom <code>spec.image</code>, set <code>spec.mysqlVersion</code> to the exact server version that image runs (for example <code>8.0.34</code>, not <code>\"8.0\"</code>). Init containers, sidecar profile selection, and <code>MY_MYSQL_VERSION</code> on pods follow that declaration before mysqld is up. Digest-only images require an explicit <code>mysqlVersion</code>. Once pods are running, <code>status.appliedMysqlVersion</code> is confirmed by SQL.</p>"},{"location":"mysql-versions-and-upgrades/#updating-images-without-rebuilding-the-operator","title":"Updating images without rebuilding the operator","text":"<ol> <li>Mount a ConfigMap (or Secret) into the operator pod at a path such as <code>/etc/mysql-operator/catalog/versions.properties</code>.</li> <li>Pass <code>--mysql-version-catalog-file=/etc/mysql-operator/catalog/versions.properties</code>.</li> <li>Restart the operator pod so it re-reads the catalog file.</li> </ol> <p>The catalog file is a list of lines <code>8.4.2=percona@sha256:...</code> (comments with <code>#</code> and blank lines are allowed).</p>"},{"location":"mysql-versions-and-upgrades/#operator-deployment","title":"Operator deployment","text":"<p>Pass <code>--sidecar-mysql84-image</code>, <code>--mysql-version-catalog-file</code>, and related flags on the operator StatefulSet. Versioned examples are in <code>deploy/manifests/</code> (see operator container <code>args</code> in <code>deploy/manifests/v0.7.0/operator/statefulset.yaml</code>).</p>"},{"location":"mysql-versions-and-upgrades/#mysql-server-major-upgrades","title":"MySQL server major upgrades","text":"<p>Changing <code>mysqlVersion</code> / <code>image</code> on an existing cluster is a data plane operation: follow Percona's upgrade documentation, take backups, and roll instances in a safe order. The operator's <code>mysql.presslabs.org/version</code> annotation refers to operator schema upgrades, not the MySQL server version.</p>"},{"location":"mysql-versions-and-upgrades/#upgrade-orchestration","title":"Upgrade orchestration","text":"<p>When <code>spec.mysqlVersion</code> changes on a cluster that already has data on PVCs, the cluster controller:</p> <ol> <li>Compares the desired version to <code>status.appliedMysqlVersion</code> (the version fully running on the data plane, confirmed by SQL).</li> <li>Legacy backfill: clusters with data but empty <code>status.appliedMysqlVersion</code> are held until the operator runs unanimous <code>SELECT VERSION()</code> on every ready mysqld pod and writes the result to status.</li> <li>Validates the upgrade path (no downgrades; one LTS line at a time, for example 8.0.x before 8.4.x). Invalid paths set the <code>UpgradeBlocked</code> condition and keep the cluster on its current version until <code>spec.mysqlVersion</code> is corrected.</li> <li>Rolls out the new pod template for valid paths (including any required init containers, for example <code>mysql-datadir-chown</code> for Percona 8.0\u21928.4).</li> <li>Sets <code>status.appliedMysqlVersion</code> only after every replica is ready, all init containers succeeded, and unanimous <code>SELECT VERSION()</code> reports the desired version.</li> </ol> <p>Patch-level bumps within the same profile line (for example <code>8.0.20</code> \u2192 <code>8.0.34</code>) follow the same rollout gates without extra steps.</p>"},{"location":"mysql-versions-and-upgrades/#cluster-mycnf-during-rollout","title":"Cluster <code>my.cnf</code> during rollout","text":"<p>The cluster ConfigMap follows the rollout target version, not <code>status.appliedMysqlVersion</code>. During a rolling upgrade, replicas still on the old mysqld version may briefly see an updated <code>my.cnf</code> until they are replaced.</p> <p>The operator does not run pre-upgrade compatibility Jobs (such as <code>mysqlsh util.checkForServerUpgrade</code>). Run Percona's upgrade checks and take backups yourself before changing <code>spec.mysqlVersion</code>.</p> <p>For a clean upgrade test: deploy on the source version (for example <code>8.0</code>), wait until <code>status.appliedMysqlVersion</code> matches and the cluster is Ready, then change <code>mysqlVersion</code> to the target (for example <code>8.4</code>).</p>"},{"location":"mysql-versions-and-upgrades/#auth-plugin-migration-80-84-manual-prerequisite","title":"Auth plugin migration (8.0 \u2192 8.4+, manual prerequisite)","text":"<p>The operator does not migrate authentication plugins. Percona/MySQL 8.4+ no longer loads <code>mysql_native_password</code>; persistent accounts still using that plugin cannot authenticate after the upgrade.</p> <p>Complete this step on the writable primary before changing <code>spec.mysqlVersion</code> to 8.4.x.</p> <p>Operator utility users (<code>sys_operator</code>, <code>sys_replication</code>, <code>sys_exporter</code>, <code>sys_heartbeat</code>, orchestrator topology user) are recreated on every mysqld start via <code>init-file</code>. On 8.4 the server default plugin is <code>caching_sha2_password</code>, so those accounts do not need manual migration.</p>"},{"location":"mysql-versions-and-upgrades/#runbook","title":"Runbook","text":"<ol> <li>Take a backup (required for any major upgrade).</li> <li>On the writable primary, list persistent accounts still on <code>mysql_native_password</code>:</li> </ol> <pre><code>SELECT user, host, plugin\nFROM mysql.user\nWHERE plugin = 'mysql_native_password'\n  AND user NOT IN ('mysql.infoschema', 'mysql.session', 'mysql.sys');\n</code></pre> <ol> <li>For each row returned, migrate on the primary (replication applies the change to secondaries):</li> </ol> <pre><code>-- When you know the password:\nALTER USER 'user'@'host' IDENTIFIED WITH caching_sha2_password BY 'password';\n\n-- When the password is unknown (for example MysqlUser CR accounts):\nALTER USER 'user'@'host' IDENTIFIED WITH caching_sha2_password RETAIN CURRENT PASSWORD;\n</code></pre> <ol> <li>Confirm replicas have caught up (for example <code>SHOW REPLICA STATUS</code>, or cluster node status <code>Replicating=True</code> and <code>Lagged=False</code>).</li> <li>Change <code>spec.mysqlVersion</code> to the 8.4 target. The operator rolls out the new image (including <code>mysql-datadir-chown</code> when upgrading Percona 8.0\u21928.4).</li> </ol>"},{"location":"mysql-versions-and-upgrades/#related-pages","title":"Related pages","text":"<ul> <li>Version profiles</li> <li>Operator configuration</li> <li>MysqlCluster \u2014 <code>UpgradeBlocked</code> condition</li> <li>Troubleshooting</li> </ul>"},{"location":"operator-configuration/","title":"Operator configuration","text":""},{"location":"operator-configuration/#operator-configuration","title":"Operator configuration","text":"<p>The operator process accepts command-line flags (and some environment variables) that control images, Orchestrator connectivity, watch scope, and security behavior. Defaults for a release are set in <code>deploy/manifests/&lt;version&gt;/operator/statefulset.yaml</code>.</p>"},{"location":"operator-configuration/#image-flags","title":"Image flags","text":"Flag Description Default (v0.7.0 manifests) <code>--sidecar-image</code> Sidecar for MySQL 5.7 <code>codecapsules-io/mysql-operator-sidecar-5.7:\u2026</code> <code>--sidecar-mysql8-image</code> Sidecar for MySQL 8.0\u20138.3 <code>codecapsules-io/mysql-operator-sidecar-8.0:\u2026</code> <code>--sidecar-mysql84-image</code> Sidecar for Percona 8.4 LTS <code>codecapsules-io/mysql-operator-sidecar-8.4:\u2026</code> <code>--metrics-exporter-image</code> mysqld_exporter image <code>prom/mysqld-exporter:v0.16.0</code> <code>--image-pull-policy</code> Default pull policy for cluster pods <code>IfNotPresent</code> <code>--image-pull-secret</code> Default pull secret name (empty) <p>Environment variable overrides exist for sidecar defaults: <code>MYSQL_OPERATOR_SIDECAR_MYSQL57_IMAGE</code>, <code>MYSQL_OPERATOR_SIDECAR_MYSQL8_IMAGE</code>, <code>MYSQL_OPERATOR_SIDECAR_MYSQL84_IMAGE</code>.</p>"},{"location":"operator-configuration/#mysql-version-catalog","title":"MySQL version catalog","text":"Flag Description <code>--mysql-versions-to-image</code> Map of <code>semver=image</code> overrides (for example <code>8.4.8=percona:8.4.8</code>) <code>--mysql-version-catalog-file</code> Path to a file of <code>semver=image</code> lines (merged after built-in defaults) <p>See MySQL versions &amp; upgrades for catalog file format and mounting via ConfigMap.</p>"},{"location":"operator-configuration/#orchestrator","title":"Orchestrator","text":"Flag / env Description <code>--orchestrator-uri</code> HTTP API base URL (default in manifests: <code>http://mysql-operator.mysql-operator/api</code>) <code>--orchestrator-topology-user</code> / <code>ORC_TOPOLOGY_USER</code> User Orchestrator uses to connect to MySQL pods <code>--orchestrator-topology-password</code> / <code>ORC_TOPOLOGY_PASSWORD</code> Password for topology user <code>--orchestrator-concurrent-reconciles</code> Orchestrator controller workers (default <code>10</code>) <p>Topology credentials are also stored in the <code>mysql-operator-orc</code> Secret. Keep them stable across upgrades \u2014 see Install the operator.</p>"},{"location":"operator-configuration/#scope-and-election","title":"Scope and election","text":"Flag Description <code>--namespace</code> Limit the operator to a single namespace (empty = all namespaces) <code>--leader-election-namespace</code> Namespace for leader election lease <code>--leader-election-id</code> Lease resource name (default <code>mysql-operator-leader-election</code>)"},{"location":"operator-configuration/#failover-and-security","title":"Failover and security","text":"Flag Default Description <code>--failover-before-shutdown</code> <code>true</code> Run Orchestrator failover in MySQL preStop hook <code>--cluster-restrict-privilege-escalation</code> <code>false</code> Set <code>allowPrivilegeEscalation: false</code> on all cluster pod containers <code>--allow-cross-namespace-user</code> <code>false</code> Allow <code>MysqlUser</code> to reference clusters in other namespaces <code>--allow-cross-namespace-database</code> <code>false</code> Allow <code>MysqlDatabase</code> to reference clusters in other namespaces <p>The privilege-escalation flag is commented out in default v0.7.0 manifests. Enable it when your policy requires stricter pod security contexts.</p>"},{"location":"operator-configuration/#metrics-and-health","title":"Metrics and health","text":"Flag Description <code>--metrics-addr</code> Operator Prometheus metrics listen address (default <code>:8080</code>) <code>--healthz-addr</code> Health probe listen address (default <code>:8081</code>) <p>Set <code>--metrics-addr=0</code> to disable operator metrics.</p>"},{"location":"operator-configuration/#editing-flags","title":"Editing flags","text":"<ol> <li>Check out or fork the repository.</li> <li>Edit <code>deploy/manifests/&lt;version&gt;/operator/statefulset.yaml</code> under the <code>operator</code> container <code>args</code> list.</li> <li>Apply: <code>kubectl apply -k deploy/manifests/&lt;version&gt;/operator</code></li> <li>Wait for rollout: <code>kubectl rollout status statefulset/mysql-operator -n mysql-operator</code></li> </ol>"},{"location":"operator-configuration/#related-pages","title":"Related pages","text":"<ul> <li>Install the operator</li> <li>Version profiles</li> </ul>"},{"location":"operator-upgrades/","title":"Operator upgrades","text":""},{"location":"operator-upgrades/#operator-upgrades","title":"Operator upgrades","text":"<p>This page covers upgrading the operator controller (the software in <code>mysql-operator</code> namespace), not MySQL server version changes on your clusters.</p>"},{"location":"operator-upgrades/#semantic-versioning-impact","title":"Semantic versioning impact","text":"Release type Typical impact Patch (for example v0.7.0 \u2192 v0.7.1) Bug fixes; operator pod may restart; clusters usually unaffected Minor (for example v0.7.x \u2192 v0.8.0) New features; operator restarts; review release notes for CRD or flag changes Major Breaking changes; may require manual steps <p>MySQL server upgrades are documented separately in MySQL versions &amp; upgrades.</p>"},{"location":"operator-upgrades/#upgrade-procedure","title":"Upgrade procedure","text":""},{"location":"operator-upgrades/#step-1-review-the-changelog","title":"Step 1 \u2014 Review the changelog","text":"<p>Read the CHANGELOG for your target version.</p>"},{"location":"operator-upgrades/#step-2-apply-new-crds","title":"Step 2 \u2014 Apply new CRDs","text":"<pre><code>kubectl apply -k deploy/manifests/&lt;new-version&gt;/crds\n</code></pre>"},{"location":"operator-upgrades/#step-3-apply-new-operator-manifests","title":"Step 3 \u2014 Apply new operator manifests","text":"<pre><code>kubectl apply -k deploy/manifests/&lt;new-version&gt;/operator\n</code></pre>"},{"location":"operator-upgrades/#step-4-wait-for-rollout","title":"Step 4 \u2014 Wait for rollout","text":"<pre><code>kubectl rollout status statefulset/mysql-operator -n mysql-operator\n</code></pre>"},{"location":"operator-upgrades/#step-5-verify-existing-clusters","title":"Step 5 \u2014 Verify existing clusters","text":"<pre><code>kubectl get mysql -A\nkubectl describe mysql &lt;name&gt;\n</code></pre>"},{"location":"operator-upgrades/#v070-notes-from-v06x-helm","title":"v0.7.0 notes (from v0.6.x / Helm)","text":"<p>Key changes when moving to this fork's v0.7.0:</p> Area Change Packaging Helm charts removed; use <code>deploy/manifests/v0.7.0</code> Images <code>ghcr.io/codecapsules-io/*</code> instead of <code>docker.io/bitpoke/*</code> MySQL 8.4 New <code>--sidecar-mysql84-image</code> flag required for 8.4 clusters mysqld_exporter Updated to v0.16.0 API <code>spec.sidecarImage</code>, <code>spec.volumeSpec.keepAfterDelete</code>, <code>status.appliedMysqlVersion</code>, <code>UpgradeBlocked</code> condition Upgrades Operator-orchestrated MySQL LTS upgrades with path validation <p>See Migrating from Helm for the Helm-specific path.</p>"},{"location":"operator-upgrades/#v063-baseline","title":"v0.6.3 baseline","text":"<p>Frozen manifests at <code>deploy/manifests/v0.6.3/</code> match the last upstream Helm chart layout. They are a reference for migration, not maintained for new features.</p>"},{"location":"operator-upgrades/#orchestrator-password-on-upgrade","title":"Orchestrator password on upgrade","text":"<p>When upgrading operator manifests, preserve orchestrator topology credentials unless you intend to rotate them cluster-wide. Changing <code>TOPOLOGY_PASSWORD</code> in <code>orchestrator-secret.yaml</code> without updating operated secrets on existing clusters breaks Orchestrator discovery.</p>"},{"location":"operator-upgrades/#crd-updates","title":"CRD updates","text":"<p><code>kubectl apply -k deploy/manifests/&lt;version&gt;/crds</code> updates CRD schemas in place. Existing custom resources remain; new optional fields become available.</p> <p>Delete CRDs only during a full uninstall when no custom resources remain.</p>"},{"location":"operator-upgrades/#related-pages","title":"Related pages","text":"<ul> <li>Install the operator</li> <li>MySQL versions &amp; upgrades</li> <li>Troubleshooting</li> </ul>"},{"location":"orchestrator/","title":"Orchestrator","text":""},{"location":"orchestrator/#orchestrator","title":"Orchestrator","text":"<p>Orchestrator manages MySQL replication topology and performs automatic failover. This operator bundles Orchestrator as a sidecar container in the <code>mysql-operator</code> StatefulSet.</p>"},{"location":"orchestrator/#what-orchestrator-does","title":"What Orchestrator does","text":"<ul> <li>Discovers cluster members and their roles (primary, replica)</li> <li>Detects primary failure and promotes a replica</li> <li>Coordinates with the operator during planned shutdowns (<code>--failover-before-shutdown</code>)</li> <li>Surfaces topology in a web UI</li> </ul> <p>Each <code>MysqlCluster</code> registers with Orchestrator using the alias <code>{cluster-name}.{namespace}</code>.</p>"},{"location":"orchestrator/#accessing-the-ui","title":"Accessing the UI","text":"<p>Port-forward the operator Service (Orchestrator HTTP is exposed on port 80):</p> <pre><code>kubectl port-forward -n mysql-operator svc/mysql-operator 8080:80\n</code></pre> <p>Open <code>http://localhost:8080</code> in your browser.</p> <p>The Service name is always <code>mysql-operator</code> in the <code>mysql-operator</code> namespace when using default manifests \u2014 unlike Helm releases, there is no release-name prefix.</p>"},{"location":"orchestrator/#cluster-conditions","title":"Cluster conditions","text":"<p><code>MysqlCluster</code> status reflects Orchestrator state:</p> Condition Meaning <code>PendingFailoverAck</code> A failover event needs acknowledgment in Orchestrator <code>FailoverInProgress</code> Orchestrator is actively failing over <p>Check with:</p> <pre><code>kubectl describe mysql my-cluster\n</code></pre>"},{"location":"orchestrator/#mysql-84-compatibility","title":"MySQL 8.4 compatibility","text":"<p>This fork ships a custom Orchestrator image built from Percona Orchestrator with MySQL 8.4 replication SQL support. See Version profiles.</p>"},{"location":"orchestrator/#topology-credentials","title":"Topology credentials","text":"<p>Orchestrator connects to MySQL using the topology user configured in <code>mysql-operator-orc</code> and propagated to each cluster's operated secret. Keep credentials consistent across operator upgrades \u2014 see Install the operator.</p>"},{"location":"orchestrator/#related-pages","title":"Related pages","text":"<ul> <li>Connecting applications \u2014 how failover affects the master Service</li> <li>Troubleshooting \u2014 orchestrator registration and password issues</li> </ul>"},{"location":"storage-and-configuration/","title":"Storage & configuration","text":""},{"location":"storage-and-configuration/#storage-and-configuration","title":"Storage and configuration","text":"<p>This page covers how the operator stores MySQL data and how you can tune server configuration.</p>"},{"location":"storage-and-configuration/#data-volumes-specvolumespec","title":"Data volumes (<code>spec.volumeSpec</code>)","text":"<p>The operator supports three volume sources for MySQL data. PersistentVolumeClaim is the recommended choice for production.</p>"},{"location":"storage-and-configuration/#persistentvolumeclaim-default","title":"PersistentVolumeClaim (default)","text":"<p>If you omit <code>volumeSpec</code>, the operator creates a PVC of 1Gi with <code>ReadWriteOnce</code> access.</p> <pre><code>apiVersion: mysql.presslabs.org/v1alpha1\nkind: MysqlCluster\nmetadata:\n  name: my-cluster\nspec:\n  secretName: my-secret\n  volumeSpec:\n    persistentVolumeClaim:\n      accessModes: [\"ReadWriteOnce\"]\n      resources:\n        requests:\n          storage: 20Gi\n      storageClassName: fast-ssd\n</code></pre> <p>Your cluster needs a default StorageClass or an explicit <code>storageClassName</code>. Without one, PVCs stay pending.</p>"},{"location":"storage-and-configuration/#retain-pvcs-on-cluster-delete","title":"Retain PVCs on cluster delete","text":"<p>Set <code>keepAfterDelete</code> when you want data volumes to survive deletion of the <code>MysqlCluster</code>:</p> <pre><code>spec:\n  volumeSpec:\n    keepAfterDelete: true\n    persistentVolumeClaim:\n      resources:\n        requests:\n          storage: 20Gi\n</code></pre> <p>Warning</p> <p>Retained PVCs are not automatically reattached to a new cluster. You must manage reuse manually.</p>"},{"location":"storage-and-configuration/#hostpath","title":"HostPath","text":"<p>For development or single-node setups:</p> <pre><code>spec:\n  volumeSpec:\n    hostPath:\n      path: /data/mysql/my-cluster\n      type: DirectoryOrCreate\n</code></pre> <p>You may need an init container to fix permissions \u2014 see <code>podSpec.initContainers</code> in <code>examples/example-cluster.yaml</code>.</p>"},{"location":"storage-and-configuration/#emptydir","title":"EmptyDir","text":"<pre><code>spec:\n  volumeSpec:\n    emptyDir: {}\n</code></pre> <p>EmptyDir is not recommended for production. Data is lost when pods are rescheduled. Support has been limited since operator v0.3.x.</p>"},{"location":"storage-and-configuration/#mysql-configuration-specmysqlconf","title":"MySQL configuration (<code>spec.mysqlConf</code>)","text":"<p>The operator generates a <code>my.cnf</code> ConfigMap with sensible defaults based on requested pod memory (for example <code>innodb-buffer-pool-size</code>). Values in <code>mysqlConf</code> override those defaults.</p> <pre><code>spec:\n  mysqlConf:\n    max_allowed_packet: 128M\n    innodb-buffer-size: 256M\n</code></pre> <p>Keys use the same names as MySQL variables (hyphens or underscores as in your <code>my.cnf</code>).</p> <p>During a version upgrade, the ConfigMap follows the rollout target version so new pods receive a compatible configuration. See MySQL versions &amp; upgrades.</p>"},{"location":"storage-and-configuration/#pod-specification-specpodspec","title":"Pod specification (<code>spec.podSpec</code>)","text":"<p>Common overrides:</p> Field Use <code>resources</code> CPU and memory requests/limits for the MySQL container <code>affinity</code> / <code>nodeSelector</code> / <code>tolerations</code> Scheduling <code>imagePullSecrets</code> Private registry credentials <code>mysqlLifecycle</code> Custom preStop hook (default includes demote-on-shutdown when enabled) <code>initContainers</code> Extra init work (for example volume permissions) <code>containers</code> Additional sidecars <code>metricsExporterResources</code> Resources for mysqld_exporter <code>mysqlOperatorSidecarResources</code> Resources for the operator sidecar"},{"location":"storage-and-configuration/#query-limits-specquerylimits","title":"Query limits (<code>spec.queryLimits</code>)","text":"<p>The operator can run pt-kill to terminate problematic queries:</p> <pre><code>spec:\n  queryLimits:\n    maxQueryTime: 300\n    maxIdleTime: 60\n    kill: oldest\n    killMode: query\n</code></pre> <p>See the pt-kill documentation for flag semantics.</p>"},{"location":"storage-and-configuration/#read-only-mode-specreadonly","title":"Read-only mode (<code>spec.readOnly</code>)","text":"<p>When <code>readOnly: true</code>, the operator attempts to make the cluster read-only. During failover there can be a short window where writes are still accepted.</p>"},{"location":"storage-and-configuration/#related-pages","title":"Related pages","text":"<ul> <li>MysqlCluster \u2014 full API reference</li> <li>MySQL version profiles \u2014 version-line defaults in <code>my.cnf</code></li> </ul>"},{"location":"troubleshooting/","title":"Troubleshooting","text":""},{"location":"troubleshooting/#troubleshooting","title":"Troubleshooting","text":"<p>Common issues when running the Code Capsules MySQL operator.</p>"},{"location":"troubleshooting/#cluster-not-ready","title":"Cluster not Ready","text":"<p>Symptoms: <code>kubectl get mysql</code> shows <code>Ready=False</code> or <code>readyNodes</code> below <code>spec.replicas</code>.</p> <p>Checks:</p> <pre><code>kubectl describe mysql &lt;cluster-name&gt;\nkubectl get pods -l mysql.presslabs.org/cluster=&lt;cluster-name&gt;\nkubectl logs &lt;pod-name&gt; -c mysql\n</code></pre> <p>Look at StatefulSet events, PVC binding, and MySQL container logs. Storage class misconfiguration is a frequent cause of pending PVCs.</p>"},{"location":"troubleshooting/#upgradeblocked-condition","title":"UpgradeBlocked condition","text":"<p>Symptoms: <code>status.conditions</code> includes <code>UpgradeBlocked=True</code> after changing <code>spec.mysqlVersion</code>.</p> <p>Cause: Invalid upgrade path \u2014 downgrades and skipping LTS lines (for example 5.7 \u2192 8.4 directly) are rejected.</p> <p>Fix: Set <code>spec.mysqlVersion</code> to the next valid step (for example <code>8.0</code> before <code>8.4</code>). See MySQL versions &amp; upgrades.</p>"},{"location":"troubleshooting/#appliedmysqlversion-lags-behind-spec","title":"appliedMysqlVersion lags behind spec","text":"<p>Symptoms: <code>spec.mysqlVersion</code> is <code>8.4</code> but <code>status.appliedMysqlVersion</code> still shows <code>8.0.x</code>.</p> <p>Cause: Rollout in progress, init container failure, or version check failing on one replica.</p> <p>Checks:</p> <pre><code>kubectl get pods -l mysql.presslabs.org/cluster=&lt;cluster-name&gt;\nkubectl describe pod &lt;pod-name&gt;   # init container status\n</code></pre> <p>Wait for all pods ready and init containers complete. For 8.0 \u2192 8.4, complete the auth plugin migration first.</p>"},{"location":"troubleshooting/#orchestrator-not-seeing-the-cluster","title":"Orchestrator not seeing the cluster","text":"<p>Symptoms: Failover does not work; Orchestrator UI empty or cluster shows errors.</p> <p>Checks:</p> <ol> <li>Cluster name not too long (Orchestrator registration limits).</li> <li>Topology password consistent between <code>mysql-operator-orc</code> secret and cluster operated secrets.</li> <li>Operator logs: <code>kubectl logs -n mysql-operator mysql-operator-0 -c operator</code></li> </ol> <p>After changing topology credentials in manifests, existing clusters may need credential alignment.</p>"},{"location":"troubleshooting/#orchestrator-password-changed-on-operator-upgrade","title":"Orchestrator password changed on operator upgrade","text":"<p>Symptoms: Orchestrator loses connection to MySQL pods after <code>kubectl apply</code> of new operator manifests.</p> <p>Cause: Topology password in <code>orchestrator-secret.yaml</code> changed while cluster secrets still hold the old password.</p> <p>Fix: Set explicit stable credentials in <code>orchestrator-secret.yaml</code> before the first production install. On upgrade, keep the same values or plan a coordinated rotation.</p>"},{"location":"troubleshooting/#authentication-failures-after-84-upgrade","title":"Authentication failures after 8.4 upgrade","text":"<p>Symptoms: Application users cannot log in after upgrading to 8.4.</p> <p>Cause: Accounts still using <code>mysql_native_password</code>.</p> <p>Fix: Run the auth migration runbook on the primary before upgrading. See MySQL versions &amp; upgrades.</p>"},{"location":"troubleshooting/#pvcs-left-behind-after-cluster-delete","title":"PVCs left behind after cluster delete","text":"<p>Symptoms: PVCs remain when you expected them gone (or vice versa).</p> <p>Cause: <code>spec.volumeSpec.keepAfterDelete</code> controls retention.</p> <p>Fix: Set <code>keepAfterDelete: true</code> only when you intend to retain data. Manually delete orphaned PVCs named <code>data-&lt;cluster&gt;-mysql-*</code> when cleaning up.</p>"},{"location":"troubleshooting/#root_password-not-set","title":"ROOT_PASSWORD not set","text":"<p>Symptoms: Cluster reconcile error mentioning <code>ROOT_PASSWORD not set in secret</code>.</p> <p>Fix: Ensure <code>spec.secretName</code> points to a Secret with <code>ROOT_PASSWORD</code> in <code>data</code> (base64-encoded).</p>"},{"location":"troubleshooting/#operator-not-reconciling-clusters","title":"Operator not reconciling clusters","text":"<p>Checks:</p> <pre><code>kubectl get pods -n mysql-operator\nkubectl logs -n mysql-operator mysql-operator-0 -c operator\n</code></pre> <p>Verify CRDs are installed (<code>kubectl get crd mysqlclusters.mysql.presslabs.org</code>). If the operator runs with <code>--namespace</code>, it only watches that namespace.</p>"},{"location":"troubleshooting/#getting-help","title":"Getting help","text":"<p>For bugs in actively maintained areas, open an issue on GitHub with reproduction steps, operator version, <code>MysqlCluster</code> YAML (redacted secrets), and relevant logs.</p> <p>See maintenance scope for what is in scope.</p>"},{"location":"troubleshooting/#related-pages","title":"Related pages","text":"<ul> <li>Orchestrator</li> <li>Operator upgrades</li> <li>Install the operator</li> </ul>"}]}